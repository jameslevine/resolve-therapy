{
  "name": "resolve-therapy-lambda",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --verbose"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-transcribe": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "jest": "^30.3.0"
  }
}
